# Render (and other hosts) set NODE_ENV=production before `npm install`,
# which makes npm silently skip devDependencies — but the TypeScript build
# needs typescript, @types/*, etc. This forces dev deps to always install.
production=false
