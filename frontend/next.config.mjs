/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "upload.wikimedia.org",
      "images.unsplash.com",
      "picsum.photos",
      "source.unsplash.com",
    ],
  },
};

export default nextConfig;
